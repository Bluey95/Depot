<% if line_item_product == @current_item %>
<tr id="current_item">
<% else %>
<tr>
<% end %>
<td><%= line_item_product.quantity %>&times;</td>
<td><%= line_item_product.productsonsale.title %></td>
<td class="item_price"><%= number_to_currency(line_item_product.total_price) %></td>
</tr>
